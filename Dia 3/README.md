# PI_5.1 Workspace

This workspace has been initialized with a simple starter structure for further development.

## Structure
- `src/` contains the initial application entry point.
- `.gitignore` excludes common editor and environment artifacts.

## Next steps
- Add your project files under `src/`.
- Update this README with project-specific details as the work evolves.
